services:
  web:
    image: composer:latest
    container_name: quiniela_web
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "8000:8000"
    command: php artisan serve --host=0.0.0.0 --port=8000
    restart: always
